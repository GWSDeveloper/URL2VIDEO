services:
  - type: web
    name: url2video
    env: python
    buildCommand: |
      apt-get update && apt-get install -y ffmpeg
      pip install yt-dlp
      npm install
    startCommand: node server.js
